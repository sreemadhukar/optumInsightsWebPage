import { TemporaryTextFormatterPipe } from './temporary-text-formatter.pipe';

describe('TemporaryTextFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TemporaryTextFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
