import { ImpactSearchPipe } from './impact-search.pipe';

describe('ImpactSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpactSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
