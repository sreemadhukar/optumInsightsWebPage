import { RlpSearchPipe } from './rlp-search.pipe';

describe('RlpSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RlpSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
