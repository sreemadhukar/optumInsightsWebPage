import { SliceStringPipe } from './slice-string.pipe';

describe('SliceStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceStringPipe();
    expect(pipe).toBeTruthy();
  });
});
